{
  "ignoreCommand": "git diff HEAD^ HEAD --name-only 2>/dev/null | grep -qE '^apps/tickets/|^packages/shared/|^packages/prisma/' && exit 1; exit 0",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
