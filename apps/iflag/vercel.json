{
  "ignoreCommand": "git fetch origin --depth=2 2>/dev/null; DIFF=$(git diff --name-only HEAD~1 HEAD -- apps/iflag/ packages/shared/ packages/prisma/ 2>/dev/null); [ -n \"$DIFF\" ] && exit 1; exit 0",
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://iventia-api.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
