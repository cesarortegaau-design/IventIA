{
  "ignoreCommand": "git fetch --deepen=1 2>/dev/null; git diff --quiet HEAD~1 HEAD -- apps/iflag/ packages/shared/ packages/prisma/ || exit 1; exit 0",
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://iventia-api.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
