{
  "ignoreCommand": "git diff HEAD^ HEAD --name-only 2>/dev/null | grep -qE '^apps/iflag/|^packages/shared/|^packages/prisma/' && exit 1; exit 0",
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://iventia-api.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
