{
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/supplier-portal/ packages/shared/ packages/prisma/",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
