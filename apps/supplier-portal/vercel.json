{
  "ignoreCommand": "git fetch origin --depth=2 2>/dev/null; DIFF=$(git diff --name-only HEAD~1 HEAD -- apps/supplier-portal/ packages/shared/ packages/prisma/ 2>/dev/null); [ -n \"$DIFF\" ] && exit 1; exit 0",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
