{
  "ignoreCommand": "DIFF=$(git show --stat HEAD -- apps/admin/ packages/shared/ packages/prisma/ 2>/dev/null); [ -n \"$DIFF\" ] && exit 1; exit 0",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
