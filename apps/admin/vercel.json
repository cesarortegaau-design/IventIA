{
  "ignoreCommand": "git fetch --deepen=1 2>/dev/null; git diff --quiet HEAD~1 HEAD -- apps/admin/ packages/shared/ packages/prisma/ || exit 1; exit 0",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
