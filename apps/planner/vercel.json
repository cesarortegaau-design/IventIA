{
  "ignoreCommand": "git fetch --deepen=1 2>/dev/null; git diff --quiet HEAD~1 HEAD -- apps/planner/ packages/shared/ packages/prisma/ || exit 1; exit 0",
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
