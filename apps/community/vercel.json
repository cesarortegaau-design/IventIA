{
  "ignoreCommand": "git fetch --deepen=1 2>/dev/null; git diff --quiet HEAD~1 HEAD -- apps/community/ packages/shared/ packages/prisma/ || exit 1; exit 0",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://api.exposaantafe.com.mx/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

