{
  "ignoreCommand": "DIFF=$(git show --stat HEAD -- apps/community/ packages/shared/ packages/prisma/ 2>/dev/null); [ -n \"$DIFF\" ] && exit 1; exit 0",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://api.exposaantafe.com.mx/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

