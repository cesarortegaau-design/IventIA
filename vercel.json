{
  "buildCommand": "pnpm install --ignore-scripts && pnpm --filter=@iventia/community build && rm -rf dist && cp -r apps/community/dist . && ls -la dist",
  "outputDirectory": "dist",
  "installCommand": "echo 'Install handled in buildCommand'",
  "nodeVersion": "20.x"
}
